{
  "id": "BUG_004",
  "name": "SLT_SIGN_CONDITIONAL_SWAP",
  "category": "Execution",
  "description": "SLT comparison swaps operands when both have same sign bit",
  "severity": "High",
  "file": "Processor/Src/ExecUnit/IntALU.sv",
  "patches": [
    {
      "line": 130,
      "original": "                `Logic( { 31'h0, ( signedOpA < signedOpB) } )",
      "buggy": "                `Logic( { 31'h0, ( (signedOpA[31] == signedOpB[31]) ? (signedOpB < signedOpA) : (signedOpA < signedOpB) ) } )"
    }
  ],
  "notes": "When both operands have the same sign (both positive or both negative), the comparison is inverted (B < A instead of A < B). Mixed-sign comparisons work correctly. This creates subtle bugs in sorting, bounds checking, and conditional logic that only manifest with specific operand combinations."
}
