{
  "id": "BUG_008",
  "name": "BYPASS_CLEAR_ON_STALL",
  "category": "Bypass",
  "description": "Bypass register incorrectly cleared during pipeline stall",
  "severity": "High",
  "file": "Processor/Src/RegisterFile/BypassNetwork.sv",
  "patches": [
    {
      "line": 29,
      "original": "        if( rst || ctrl.clear ) begin              // rst ",
      "buggy": "        if( rst || ctrl.clear || ctrl.stall ) begin              // rst "
    }
  ],
  "notes": "Adds stall condition to bypass register clear logic. During pipeline stalls, bypass data is incorrectly cleared, causing dependent instructions to receive zero instead of forwarded values when pipeline resumes."
}
